services:
  amd64-runner:
    profiles:
      - amd64
    extends:
      file: .reusable-runner.yml
      service: runner

  arm64-runner:
    profiles:
      - arm64
    platform: linux/arm64
    extends:
      file: .reusable-runner.yml
      service: runner
